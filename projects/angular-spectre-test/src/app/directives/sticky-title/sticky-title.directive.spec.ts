import { StickyTitleDirective } from './sticky-title.directive';

describe('StickyTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyTitleDirective();
    expect(directive).toBeTruthy();
  });
});
